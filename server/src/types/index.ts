export interface FileMetadata {
  fileId: string;
  fileName: string;
  createdAt: string;
  modifiedAt: string;
  size: number;
  mimeType: string;
  status: string;
  ownerId: string;
  hash: string;
}
